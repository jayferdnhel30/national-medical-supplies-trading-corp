# TODO

- [ ] Add/update `menuItems` usage in `src/shared/components/layout/PublicLayout.tsx` so menu labels and routes are centralized.
- [ ] Ensure mobile drawer and desktop nav render from `menuItems`.
- [ ] Run typecheck/build (npm test/build) to verify no TS/ESLint errors.
